function ajaxValidationRecaptcha(task, formId, data, componentId) {	switch (task) {		case 'beforeSend':			var form = rsfp_getForm(formId);			var container = document.getElementById('recaptchaContainer' + componentId);			data.params.push('recaptcha_response_field=' + encodeURIComponent(container.getElementById('recaptcha_response_field').value));			data.params.push('recaptcha_challenge_field=' + encodeURIComponent(container.getElementById('recaptcha_challenge_field').value));		break;				case 'afterSend':			if (data.response[1] && data.response[1].indexOf(componentId) > -1) {				Recaptcha.reload();			}		break;	}}